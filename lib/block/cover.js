// Generated by CoffeeScript 1.9.2
(function() {
  var equality, existence;

  equality = require("./helpers/equality");

  existence = require("./helpers/existence");

  module.exports = function(chai, utils) {
    var property;
    property = "cover";
    equality.call(this, chai, utils, property);
    existence.call(this, chai, utils, property);
    return chai.Assertion.addProperty("orientation", function() {
      var block, cover;
      block = this._obj;
      cover = block.cover;
      return this.assert(cover.orientation != null, "expected block " + block.id + " to have a cover with an orientation", "expected block " + block.id + " to have a cover without an orientation");
    });
  };

}).call(this);
